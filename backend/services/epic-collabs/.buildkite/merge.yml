steps:
  - label: 'Deploy'
    trigger: 'epic-collabs-deploy'
    build:
      commit: '$BUILDKITE_COMMIT'
      message: '$BUILDKITE_MESSAGE'
      branch: '$BUILDKITE_BRANCH'
      env:
        STAGE: 'production'
        ROLE_ARN: arn:aws:iam::12345:role/sp-buildkite-test
