import { colours } from 'styles';
import { Styled } from 'theme-ui';
import styled from 'styled-components';

const SectionTitle = styled(Styled.h1)`
  color: ${colours.darkGrey900};
`;

export { SectionTitle };
